[package]
name = "godot-lru-rust"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]  # Compile this crate to a dynamic C library.

[dependencies]
godot = "0.5.3"
moka = { version = "0.12.15", features = ["sync"] }
uuid = "1.23.3"
