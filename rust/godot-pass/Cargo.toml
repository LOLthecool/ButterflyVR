[package]
name = "godot-pass"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
argon2 = "0.5.3"
godot = { version = "0.4.5", features = ["experimental-threads"] }
password-hash = {version = "0.5.0", features = ["std"]}
